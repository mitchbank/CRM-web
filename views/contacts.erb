<h1>Contacts</h1>

<ul>
	<% $rolodex.contacts.each do |contact| %>
	<li> <a href="/contacts/<%= contact.id %>"> <%= contact.first_name %> <%= contact.last_name %></a> <%= contact.email %> <%= contact.notes %> 
</form>
	</li>
	<% end %>
</ul>